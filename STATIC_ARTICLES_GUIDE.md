# 桃白簪花 · 静态文字文章发布说明

GitHub Pages 版本只发布**纯文字文章**，不包含图片上传、前端密码或访问令牌。文章数据统一放在根目录的 `articles.js`；通过 GitHub 网页编辑并提交后，GitHub Pages 会按仓库原有发布流程更新站点。

## 发布一篇文章

1. 打开仓库中的 [`articles.js`](./articles.js)，点击右上角的铅笔图标。
2. 在 `window.TAOBEI_ARTICLES = [` 与 `];` 之间添加一篇文章对象。最新文章应放在数组最前面。
3. 点击 **Commit changes** 保存。请勿修改文件顶部的说明文字，也不要在公开文件中填写密码、令牌或私密链接。

```js
window.TAOBEI_ARTICLES = [
  {
    id: "20260817-first-scroll",
    date: "2026.08.17",
    category: "书庭新卷",
    title: "在这里填写文章标题",
    summary: "这里填写一段短摘要，用于首页文章卡片。",
    paragraphs: [
      "这里是第一段正文。",
      "这里是第二段正文。"
    ]
  }
];
```

> `id` 必须唯一，推荐使用日期加英文短名；`paragraphs` 中的每个字符串对应阅读长卷中的一个段落。请使用英文双引号，并在段落之间保留逗号。

## 修改或撤回

修改已发布文章时，直接编辑对应对象后提交。需要撤回文章时，将该对象从数组中完整删除，并注意保留其余对象之间的逗号与方括号。
